---
import { Image } from "astro:assets";
import conusesImg from "../assets/img/screen4_conuses.jpg";
import wheelImg from "../assets/img/screen4_wheel.png";
import screen4Blue from "../assets/img/screen4_blue.png";
import screen4Red from "../assets/img/screen4_red.png";
---

<div class="screen4" id="for-who">
  <div class="first-section">
    <div
      class="first-section__content fade-in-left"
      style={`background-image: url(${screen4Blue.src});`}
    >
      <div class="pr-4">
        <h2 class="screen__title">Если ты:</h2>
        <ul class="wheel-list">
          <li><span>Не понимаешь, как ведет себя машина</span></li>
          <li><span>Чувствуешь себя неуверенно за рулем</span></li>
          <li><span>Боишься быстро ездить и маневрировать</span></li>
        </ul>
      </div>
    </div>
    <Image src={conusesImg} alt="conuses" class="fade-in-right h-full" />
  </div>
  <section class="second-section">
        <Image src={wheelImg} alt="wheel" class="fade-in-left"/>
        <div class="second-section__content fade-in-right" style={`background-image: url(${screen4Red.src});`}>
            <div class="pl-4">
                <h2 class="screen__title">Или наоборот,</h2>
                <ul class="wheel-list">
                    <li>Думаешь, что все знаешь и умеешь</li>
                    <li>Хочешь узнать пределы возможностей автомобиля</li>
                    <li>Ищешь, как попасть в автоспорт</li>
                </ul>
            </div>
        </div>
    </section>
  <!--<div class="screen4__title-wrapper fade-in-left">-->
  <!--  <p class="screen4__title">тебе срочно нужно ратата</p>-->
  <!--  <button class="link open-ratatat-modal-js">Что такое ратата?</button>-->
  <!--</div>-->
</div>

<div id="ratataModal" class="modal">
  <div class="modal-content">
    <span class="close" id="ratataModalclose">&times;</span>
    <div class="ratata-conent">
      <p>
        Вот знаешь, иногда просыпаешься с&nbsp;таким интересным, игривым настроением. Хочется сесть в машину и не просто ехать, а суетить и раздавать. Вот это настроение мы и называем «ратата». Но мы хорошие мальчики и девочки, ПДД соблюдаем и аварийных ситуаций не создаем. Зато на полигоне можем творить любую ратату — и возвращаться на&nbsp;дороги нормальным человеком.
      </p>
    </div>
  </div>
</div>

<style>

  .ratata-conent {
    width: 360px;
    background: #fff;
    padding: 40px 24px;
  }
  .screen4 {
    max-width: 100%;
    overflow-x: hidden;
    padding-top: 90px;
  }

  .screen4__title-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-bottom: 0;
  }

  .screen4__title {
    padding: 0 40px;
    font-family: var(--heading-font);
    text-transform: uppercase;
    text-align: center;
    font-weight: bold;
    line-height: 1.2;
    color: var(--color-accent);
    font-size: 70px;
    margin-bottom: 30px;
  }

  .screen4__title::after {
    content: "";
    display: block;
    height: 25px;
    background: linear-gradient(
      270.01deg,
      #53abff 0.01%,
      rgba(83, 171, 255, 0) 99.41%
    );
    width: 100%;
  }

  .screen__title {
    color: inherit;
    margin-bottom: 35px;
  }

  .first-section {
    margin-bottom: 30px;
  }
  .second-section {
  }

  .first-section,
  .second-section {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .first-section__content {
    display: flex;
    align-items: center;
    padding-right: 35%;
    justify-content: flex-end;
    color: var(--color-contrast);
    background-repeat: no-repeat;
    background-size: cover;
    background-position: right;
    background-color: var(--color-accent);
    clip-path: polygon(0% 0%, 100% 0%, 70% 100%, 0% 100%);
    margin-right: -35%;
  }
  .first-section img {
    clip-path: polygon(40% 0%, 100% 0%, 100% 100%, 0% 100%);
    width: 100%;
  }

  img {
    max-width: 100%;
    object-fit: cover;
  }

  .second-section__content {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    padding-left: 35%;
      background-size: 123%;
    color: var(--color-contrast);
    background-repeat: no-repeat;
    background-position: left;
    background-color: var(--color-accent);
    clip-path: polygon(7% 0, 100% 0%, 100% 100%, 29% 100%);
    margin-left: -35%;
  }

  .second-section img {
    clip-path: polygon(0% 0%, 70% 0%, 100% 100%, 0 100%);
    width: 100%;
  }

  @media (max-width: 1023px) {
    .first-section {
        grid-template-columns: 1fr;
        grid-auto-rows: 400px;
    }

    .first-section__content  {
        padding: 40px 40px;
        justify-content: center;
        align-items: center;
        clip-path: polygon(0 0, 100% 0, 100% 83%, 0 100%);
        margin-right: 0;
        padding-right: 0;
        padding-top: 0;
        margin-bottom: -20%;
        position: relative;
        z-index: 1;
        background-size: cover;
    }

    .first-section img {
        clip-path: none;
    }

    .second-section {
        grid-template-columns: 1fr;
        grid-template-rows: 450px 300px;
        margin-bottom: 80px;
    }

    .second-section__content  {
        padding: 0 40px;
        justify-content: center;
        align-items: center;
        clip-path: polygon(0 0, 100% 0, 100% 83%, 0 100%);
        margin-right: 0;
        padding-right: 0;
        padding-top: 0;
        margin-bottom: -20%;
        position: relative;
        z-index: 1;
        order: -1;
        margin-left: 0;
        background-size: cover;
    }

    .second-section img {
        clip-path: none;
        height: 300px;
    }

    .screen4__title-wrapper {
        padding-top: 100px;
    }

    .screen4__title {
        font-family: var(--heading-font);
        text-transform: uppercase;
        text-align: center;
        font-weight: bold;
        line-height: 38.5px;
        color: var(--color-accent);
        font-size: 35px;
        margin-bottom: 30px;
    }
  }
</style>

<script>
  var modal = document.getElementById("ratataModal");

var btns = document.querySelectorAll(".open-ratatat-modal-js");

var span: HTMLElement = document.getElementById(
  "ratataModal"
) as HTMLElement;

btns.forEach((btn: HTMLElement) => {
  btn.onclick = function () {
    modal.style.display = "flex";
  };
});

span.onclick = function () {
  modal.style.display = "none";
};

window.onclick = function (event) {
  if (event.target == modal) {
    modal.style.display = "none";
  }
};
</script>
